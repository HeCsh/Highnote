[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every path serves index.html so client-side routes
# (/dashboard, /onboarding, /r/:slug) resolve on direct load and refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
